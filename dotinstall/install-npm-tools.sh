#! /bin/bash

# TODO: move node_modules to a  global place
npm i diff-so-fancy
npm i git-trim
sudo npm install typescript -g
sudo npm install -g wsl-open
sudo npm install --global generate generate-license
